{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splashscreen-local",
  "description": "Local splash window with no frontend command permissions.",
  "local": true,
  "windows": ["splashscreen"],
  "permissions": [
    "core:event:allow-listen",
    "core:window:allow-start-dragging"
  ]
}
